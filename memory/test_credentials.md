# Test Credentials

## ScoutKrew — Dummy Auth (no real backend auth)
- URL: /auth (click "Get Started" on the landing page)
- Email: any value works (e.g. player@scoutkrew.gg)
- Password: any value works (e.g. password123)
- Google button: also dummy — navigates straight into the app
- App routes after "login": /app/home, /app/explore, /app/chat, /app/matches, /app/profile

All data is dummy/mocked (clips, users, chats, matches). No real auth or persistence yet.
